{
  "$schema": "https://anthropic.com/claude-code/marketplace.schema.json",
  "name": "orrgal1",
  "description": "orrgal1 — self-contained, repo-agnostic Claude Code plugins (forge, diagnose, ralph, persona, reviewable, devloop)",
  "owner": {
    "name": "orrgal1"
  },
  "plugins": [
    {
      "name": "forge",
      "description": "PR forge chain: /forge-setup maps host-repo tooling into $FORGE_HOME (~/.claude/forge/<repo-key>/), then the chain drives a PR from a source brief through goals, scenarios, tests, design, impl, continuous CI, and lens-designed review to READY — then hands off to peer review. Depends on @orrgal1/devloop for /restack.",
      "source": "./forge",
      "category": "development"
    },
    {
      "name": "diagnose",
      "description": "Portable, repo-agnostic diagnostic toolkit — /root-cause, /hypothesize, /pepper, /trace. No dependency on other plugins.",
      "source": "./diagnose",
      "category": "development"
    },
    {
      "name": "ralph",
      "description": "/ralph — general bounded-autonomy iteration loop toward any verifiable target; grinds to green, commits each step, stops at success or budget. Self-contained, repo-agnostic.",
      "source": "./ralph",
      "category": "development"
    },
    {
      "name": "persona",
      "description": "Simple inlined persona — /setup-persona writes ~/.claude/persona.md (idempotent) and ensures CLAUDE.md @-imports it; /load-persona swaps in a named persona from the builtin pool. No dependency on other plugins.",
      "source": "./persona",
      "category": "productivity"
    },
    {
      "name": "reviewable",
      "description": "Reviewable.io automation via agent-browser — /reviewable-read, /reviewable-reply, /reviewable-publish, /reviewable-login. Repo-agnostic (resolves owner/repo from git origin).",
      "source": "./reviewable",
      "category": "development"
    },
    {
      "name": "devloop",
      "description": "Thin stacked-PR dev-loop skills — /restack (bring a PR's base into its branch, merge/rebase per operator preference, syncing base with upstream first), /restack-all (same across a stack, bottom-up), /deslop (strip AI slop from a PR diff, reusing /simplify). Infers PR topology from GitHub, falls back to raw git. No dependency on other plugins.",
      "source": "./devloop",
      "category": "development"
    },
    {
      "name": "graphify-wrapper",
      "description": "Thin harness over the graphify knowledge-graph CLI for monorepos + worktrees — /graphify-wrapper-setup, /graphify-wrapper-map (analyze & propose domains to index), /graphify-wrapper-index (register named domains), /graphify-wrapper-sync (on-demand; AST default + --semantic via claude-cli; seeds worktrees from main), /graphify-wrapper-query, /graphify-wrapper-status. Graphs live in-tree (gitignored, never pushed); only the registry lives centrally. No dependency on other plugins.",
      "source": "./graphify-wrapper",
      "category": "development"
    },
    {
      "name": "welcome",
      "description": "Onboarding for the orrgal1 marketplace — /welcome runs an interactive, idempotent walkthrough: prerequisites (gh, SSH remotes), recommended 3rd-party plugins (claude-mem, caveman, context7, official bundle), persona, graphify-wrapper, per-repo forge setup, then prints a usage guide for every orrgal1 plugin. No dependency on other plugins.",
      "source": "./welcome",
      "category": "productivity"
    }
  ]
}
