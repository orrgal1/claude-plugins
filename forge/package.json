{
  "name": "@orrgal1/forge",
  "version": "4.13.0",
  "description": "Self-contained PR forge chain — no dependency on other plugins; map host-repo tooling via /forge-setup into $FORGE_HOME (default ~/.claude/forge/<repo-key>/) so worktrees share state. setup (/forge-setup, --migrate user) + start (/forge-start) + atoms (/forge-goals, /forge-scenarios, /forge-validations, /forge-tests, /forge-design, /forge-audit, /forge-review) + per-layer attestation skills (/forge-verify-goals, /forge-verify-scenarios, /forge-verify-tests, /forge-verify-match, /forge-verify-runs, /forge-verify-validations) + fix-loops (/forge-impl-green, /forge-audit-green, /forge-ci-green, /forge-review-green) + orchestrator (/forge) + domain mapping (/forge-map: db, api, events, config, ad-hoc; ground truth + branch divergence + merge absorption) + multi-channel PR review (/forge-review: lens-fanout + always-on built-in /code-review + /security-review wrappers, custom via $FORGE_HOME/review-channels/) + tool packaging (/forge-tool: capture ad-hoc flows under $FORGE_HOME/tools/<name> as script / instructions / dir / agent forms; first-class cross-skill lookup). /forge drives start → goals (AWAIT_GOALS_REVIEW) → design (AWAIT_DESIGN_REVIEW) → scenarios (AWAIT_SCENARIOS_REVIEW) → tests → impl → audit-green → ci-green → review-green → final-ci → READY. Modes: auto | manual. Resume via /forge approve and /forge iterate \"<feedback>\". Bundles its own lens pool + review channels (extensible via $FORGE_HOME/; no persona bundled — the tiered baseline is the default). Per-PR artifacts under .pr-artifacts/<branch-slug>/forge/. Repo state under $FORGE_HOME (legacy <repo>/.forge/ honored during the migration window).",
  "author": "orrgal1",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/orrgal1/forge.git"
  },
  "keywords": [
    "claude-code-plugin",
    "forge",
    "tdd",
    "scenarios",
    "pr-review"
  ],
  "files": [
    ".claude-plugin",
    "skills",
    "agents",
    "lenses",
    "personas",
    "review-channels"
  ]
}
